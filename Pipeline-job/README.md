# Hello World Dockerized Flask App

This is a simple Flask web application that runs inside a Docker container and is deployed using a Jenkins pipeline.

## Steps to Run

1. Clone the repository
2. Build Docker image:
